{
  "name": "rollie",
  "version": "0.2.1",
  "description": "Dust helper for use with the Rollbar client",
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "npm run build",
    "build": "babel src --out-dir dist --copy-file",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Amos",
  "repository": {
    "type": "git",
    "url": "https://github.com/eltonjuan/rollie"
  },
  "license": "MIT",
  "devDependencies": {
    "babel": "^6.1.18"
  }
}
